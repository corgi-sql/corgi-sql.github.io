# dev, staging, prod
ENVIRONMENT=dev
BACKEND_URL=http://localhost:8000
AUTH_SECRET= # Generate a secret key using the command: npx auth secret
AUTH_TRUST_HOST=true # Set to true for local production builds
AUTH_GOOGLE_ID={CLIENT_ID}
AUTH_GOOGLE_SECRET={CLIENT_SECRET}
AUTH_URL=http://localhost:3000/api/auth